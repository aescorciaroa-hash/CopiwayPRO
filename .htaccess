<IfModule mod_rewrite.c>
    RewriteEngine On

    # Si Laragon apunta el DocumentRoot a la raiz del proyecto en vez de /public,
    # esta regla reenvia todo hacia la carpeta public.
    RewriteCond %{REQUEST_URI} !^/public/
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>
